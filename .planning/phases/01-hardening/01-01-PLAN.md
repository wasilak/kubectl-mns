---
phase: 01-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kubectl-mns
autonomous: true
requirements:
  - SAFETY-01
  - SAFETY-02
  - SAFETY-03
  - BUGFIX-02
  - BUGFIX-03
  - BUGFIX-04

must_haves:
  truths:
    - "shellcheck kubectl-mns exits 0 with no warnings or errors"
    - "kubectl is invoked via a bash array, not a string-concatenation variable"
    - "All array expansions in namespaces loop and arg-build are double-quoted"
    - "usage() prints all 6 echo lines exclusively to stderr"
    - "Running the plugin with no args exits with code 1, not 0"
    - "usage() shows namespace-2, not namespac-2"
  artifacts:
    - path: "kubectl-mns"
      provides: "Hardened bash plugin — shell-safe, bug-free"
      contains: "\"${namespaces[@]}\""
  key_links:
    - from: "kubectl-mns:run_kubectl()"
      to: "kubectl"
      via: "bash array exec"
      pattern: "\"\\$\\{kubectl_cmd\\[\\@\\]\\}\""
    - from: "kubectl-mns:usage()"
      to: "stderr"
      via: ">&2 redirect on every echo"
      pattern: "echo.*>&2"
---

<objective>
Harden the kubectl-mns bash plugin: fix two unquoted array expansions (SC2068), replace the string-concatenation kubectl build with an array exec, upgrade the empty-string test to `-n "$data"`, fix the namespac-2 typo in usage(), redirect all usage() output to stderr, and make the no-args exit code 1.

Purpose: Eliminate word-splitting vulnerabilities, shellcheck errors, and user-visible bugs so the plugin is correct and safe before new features are added.
Output: A modified `kubectl-mns` that passes `shellcheck` at exit 0 and satisfies all six requirements.
</objective>

<execution_context>
@/Users/piotrek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hardening/01-RESEARCH.md

<interfaces>
<!-- Current kubectl-mns structure — extracted for executor reference. -->
<!-- Read the actual file via read_first before editing. -->

Key locations in kubectl-mns (line numbers from current file):
- Line 5–13  : usage() function — 6 echo calls, currently all to stdout
- Line 7     : typo "namespac-2" (missing 'e')
- Line 45    : for ns in ${namespaces[@]}   — UNQUOTED (SC2068, SAFETY-01)
- Line 48    : for command_arg in ${actual_kubectl_args[@]}  — UNQUOTED (SC2068, SAFETY-01)
- Line 46–53 : string-concatenation kubectl_command build (SAFETY-02)
- Line 54    : data=$($kubectl_command)  — string exec (SAFETY-02)
- Line 55    : if [[ ! -z $data ]]; then  — double-negation, unquoted (SAFETY-03)
- Line 63    : if [ -z "$1" ]; then usage && exit;   — bare exit = exit 0 (BUGFIX-04)
- Line 64–65 : -h / --help paths use bare exit (correct — must remain exit 0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix shell safety — quote array expansions and rewrite kubectl exec as array</name>
  <files>kubectl-mns</files>
  <read_first>
    - kubectl-mns (read in full — see current loop structure at lines 45–59 before touching anything)
    - .planning/phases/01-hardening/01-RESEARCH.md (Architecture Patterns section: "Bash Array Invocation" and "Full corrected run_kubectl() inner loop")
  </read_first>
  <action>
    In run_kubectl(), replace the outer namespace loop and inner arg-build with the array-exec pattern (SAFETY-01 + SAFETY-02 + SAFETY-03):

    1. Change line 45 from `for ns in ${namespaces[@]}` to `for ns in "${namespaces[@]}"` — fixes SC2068 on namespaces array.

    2. Replace lines 46–54 (the string-concatenation kubectl_command build + string exec) with a single local array declaration and array exec:
       - Declare `local kubectl_cmd=("kubectl" "${actual_kubectl_args[@]}" --namespace "$ns")` — this also fixes SC2068 on actual_kubectl_args (SAFETY-01) and uses array exec (SAFETY-02).
       - Execute via `data=$("${kubectl_cmd[@]}")`.
       - The inner `for command_arg in ...` loop and the `kubectl_command` string variable are removed entirely.

    3. Change line 55 from `if [[ ! -z $data ]]; then` to `if [[ -n "$data" ]]; then` (SAFETY-03). The variable must be double-quoted.

    After the edit, the loop body in run_kubectl() should contain exactly: the local array declaration, the array exec assignment to data, and the -n check with echo.

    Do NOT change lines 64–65 (-h / --help exit paths).
  </action>
  <verify>
    <automated>shellcheck /Users/piotrek/git/kubernetes/kubectl-mns/kubectl-mns</automated>
  </verify>
  <acceptance_criteria>
    - `shellcheck kubectl-mns` exits 0 with no output (zero SC2068 errors, zero other warnings)
    - `rg '\$\(kubectl_command\)' kubectl-mns` returns no matches (string exec is gone)
    - `rg '"?\$\{kubectl_cmd\[@\]\}"' kubectl-mns` matches exactly one line (the array exec line)
    - `rg '"?\$\{namespaces\[@\]\}"' kubectl-mns` matches exactly one line, and that line starts with `for ns in "`
    - `rg -- '! -z' kubectl-mns` returns no matches
    - `rg -- '-n "\$data"' kubectl-mns` matches exactly one line
  </acceptance_criteria>
  <done>shellcheck exits 0; kubectl is invoked via array; both array expansions are quoted; -n "$data" replaces ! -z $data</done>
</task>

<task type="auto">
  <name>Task 2: Fix usage() — stderr redirect, namespace-2 typo, and no-args exit code</name>
  <files>kubectl-mns</files>
  <read_first>
    - kubectl-mns (read in full — focus on lines 5–13 for usage() and line 63 for no-args guard)
    - .planning/phases/01-hardening/01-RESEARCH.md (Architecture Patterns: "usage() to stderr" and "No-args guard with exit 1"; Common Pitfalls 3 and 5)
  </read_first>
  <action>
    Three surgical changes (BUGFIX-02, BUGFIX-03, BUGFIX-04):

    1. BUGFIX-02: On line 7, change `namespac-2` to `namespace-2` in the usage() echo string.

    2. BUGFIX-03: Add `>&2` redirect to all 6 echo calls inside usage() (lines 6–12). Each `echo "..."` becomes `echo "..." >&2`. There are exactly 6 echo lines in usage() — count them after the edit to confirm none were missed.

    3. BUGFIX-04: On line 63, change `usage && exit` to `usage && exit 1`. Only this line — the -h (line 64) and --help (line 65) paths must remain bare `exit` (which is exit 0).

    Do NOT add >&2 to echo calls outside usage(). Do NOT change line 64 or 65.
  </action>
  <verify>
    <automated>
      shellcheck /Users/piotrek/git/kubernetes/kubectl-mns/kubectl-mns &&
      bash /Users/piotrek/git/kubernetes/kubectl-mns/kubectl-mns 2>/dev/null; echo "stdout_exit:$?" &&
      bash /Users/piotrek/git/kubernetes/kubectl-mns/kubectl-mns >/dev/null 2>&1; echo "exit_code:$?"
    </automated>
  </verify>
  <acceptance_criteria>
    - `rg 'namespac-2' kubectl-mns` returns no matches (typo gone)
    - `rg 'namespace-2' kubectl-mns` returns exactly 1 match (correct spelling present)
    - `rg -c '>&2' kubectl-mns` returns 6 (all 6 usage() echo calls redirect to stderr)
    - `bash kubectl-mns 2>/dev/null` produces no stdout output (usage goes to stderr only)
    - `bash kubectl-mns >/dev/null 2>&1; echo $?` prints 1 (no-args exit code is 1)
    - `bash kubectl-mns -h >/dev/null 2>&1; echo $?` prints 0 (help exit code unchanged)
    - `bash kubectl-mns --help >/dev/null 2>&1; echo $?` prints 0 (help exit code unchanged)
    - `shellcheck kubectl-mns` exits 0 (no regressions from the redirects)
  </acceptance_criteria>
  <done>usage() outputs 6 lines to stderr; namespace-2 spelled correctly; no-args exits 1; help exits 0; shellcheck clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→plugin | Namespace names and kubectl args come from user shell input — treated as untrusted until safely passed via array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | run_kubectl() namespace/arg expansion | mitigate | Quote all array expansions `"${arr[@]}"` + array exec eliminates word-splitting on attacker-controlled namespace names |
| T-1-02 | Elevation | No-args exit code | accept | Exit 0 on no-args is a usability bug, not a security threat; BUGFIX-04 fixes it for correctness |
</threat_model>

<verification>
Run after both tasks complete:

```bash
shellcheck kubectl-mns
rg 'namespac-2' kubectl-mns        # expect: no output
bash kubectl-mns 2>/dev/null       # expect: no stdout output
bash kubectl-mns >/dev/null 2>&1; echo "exit: $?"  # expect: exit: 1
bash kubectl-mns -h >/dev/null 2>&1; echo "exit: $?"  # expect: exit: 0
```
</verification>

<success_criteria>
- shellcheck kubectl-mns exits 0 with zero diagnostics
- kubectl-mns invokes kubectl via array exec (no string-concatenation variable)
- Both array expansions are quoted: "${namespaces[@]}" and "${actual_kubectl_args[@]}"
- Data check uses -n "$data" (no ! -z anywhere)
- usage() has 6 echo lines, all redirected >&2
- usage() shows "namespace-2" (not "namespac-2")
- Running with no args exits 1 and prints nothing to stdout
- Running with -h or --help exits 0
</success_criteria>

<output>
Create `.planning/phases/01-hardening/01-01-SUMMARY.md` when done
</output>
