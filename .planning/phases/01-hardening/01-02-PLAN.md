---
phase: 01-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - BUGFIX-01

must_haves:
  truths:
    - "README.md usage example shows the correct command without redundant kubectl after --"
  artifacts:
    - path: "README.md"
      provides: "Corrected usage documentation"
      contains: "kubectl mns ns1 ns2 ns3 -- get pods"
  key_links:
    - from: "README.md line 45"
      to: "correct usage"
      via: "remove redundant kubectl token after --"
      pattern: "-- get pods"
---

<objective>
Fix the README.md usage example: remove the redundant `kubectl` token after `--`. The current example shows `kubectl mns ns1 ns2 ns3 -- kubectl get pods`, which incorrectly implies the user must type `kubectl` twice. The correct form is `kubectl mns ns1 ns2 ns3 -- get pods`.

Purpose: Prevent users from invoking the plugin incorrectly based on the documentation example.
Output: One-line change in README.md.
</objective>

<execution_context>
@/Users/piotrek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/piotrek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-hardening/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove redundant kubectl from README usage example</name>
  <files>README.md</files>
  <read_first>
    - README.md (read in full — target is the shell code block at line 44–46)
  </read_first>
  <action>
    On line 45 of README.md, change:
      `kubectl mns ns1 ns2 ns3 -- kubectl get pods`
    to:
      `kubectl mns ns1 ns2 ns3 -- get pods`

    This is the only line to change. Do not reformat or alter any other content in README.md.
  </action>
  <verify>
    <automated>rg "-- kubectl get" /Users/piotrek/git/kubernetes/kubectl-mns/README.md; echo "exit:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `rg '-- kubectl get' README.md` returns no matches (redundant kubectl gone)
    - `rg '-- get pods' README.md` returns exactly 1 match (correct form present)
    - No other lines in README.md changed (diff shows exactly 1 line modified)
  </acceptance_criteria>
  <done>README.md usage example shows "kubectl mns ns1 ns2 ns3 -- get pods" with no redundant kubectl after --</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs→user | README content is trusted; no security boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03 | — | README.md | accept | Documentation-only change; no executable code or trust boundary involved |
</threat_model>

<verification>
```bash
rg '-- kubectl get' README.md   # expect: no output
rg '-- get pods' README.md      # expect: 1 match
```
</verification>

<success_criteria>
- README usage example line reads: `kubectl mns ns1 ns2 ns3 -- get pods`
- No other README.md content changed
</success_criteria>

<output>
Create `.planning/phases/01-hardening/01-02-SUMMARY.md` when done
</output>
